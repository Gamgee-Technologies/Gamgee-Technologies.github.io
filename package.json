{
  "name": "gamgee-website",
  "private": true,
  "scripts": {
    "build:shell": "esbuild src/site-shell.jsx --bundle --minify --format=iife --target=es2020 --outfile=site/assets/site-shell.js",
    "build": "npm run build:shell"
  },
  "dependencies": {
    "react": "^19.1.1",
    "react-dom": "^19.1.1"
  },
  "devDependencies": {
    "esbuild": "^0.25.9"
  }
}
